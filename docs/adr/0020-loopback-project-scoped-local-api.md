# 本地 API 仅限 loopback 与项目能力

本地应用只监听当前设备的 loopback 地址，只允许本项目 Web 体验访问，并仅暴露读取允许展示的代码、加载指定位置 `.pth`、执行推理以及返回网络契约和轨迹等固定能力；它不提供任意路径文件读取、任意 Python 或 shell command 执行，也不向局域网或公网开放。所有运行所需网页资产随仓库交付且不依赖 CDN，具体端口、授权和通信协议在实现规格中确定。
