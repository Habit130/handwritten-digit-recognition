# 模型产物采用单个 PTH state_dict 文件

首个版本将模型产物定义为一个 `.pth` 文件，其中保存标准网络结构的 `state_dict`，而不是序列化完整模型对象或学习者的可执行代码；训练和推理共享同一份标准网络结构定义，训练完成后保存该文件，真实实验创建标准模型实例并加载它。训练与推理都会执行同一个 `forward()`，但训练还会计算 loss、执行 backward propagation 和参数更新，推理则在 evaluation mode 下关闭梯度计算并采集实时轨迹。
