# 显式失败且不提供静默 fallback

环境、模型、固定资产或推理失败时立即停止相应操作，网页给出简洁中文说明、失败阶段和可执行修复方向，同时保留原始 command 与 error message；学习者修复后可以直接重试。系统不能在学习者 `.pth` 失败时静默改用预训练模型，不能在数据集缺失时替换为示例数据，也不能自动改写学习者代码或模型文件，因为这些行为会隐藏真实问题并破坏学习结果的可信度。
