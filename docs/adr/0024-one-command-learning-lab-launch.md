# 使用一个命令启动完整学习实验室

所有挑战路线从项目根目录的单一平台入口启动。入口在首次运行时自动创建唯一的项目 `.venv` 并安装锁定依赖，后续直接复用该环境，然后启动本地应用层、提供已构建的 Web 体验、检查必要固定资产并输出或打开本地地址；学习者不需要激活环境，也不需要分别启动 frontend dev server 与 Python service。模型训练保持为独立命令，不由该入口代办，从而简化产品启动而不隐藏真实训练工作流。
